# ProjectGoat Environment Configuration
# Copy this file to .env and update values for your deployment

# =============================================================================
# ENVIRONMENT
# =============================================================================
# Options: development, production
# Default: development
ENVIRONMENT=development

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Host to bind to
# Default: 127.0.0.1 (localhost only)
# Production: 0.0.0.0 (all interfaces)
HOST=127.0.0.1

# Port to listen on
# Default: 8000
PORT=8000

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Database connection URL
# Default: sqlite:///projectgoat.db (local file-based database)
# PostgreSQL example: postgresql://user:password@localhost:5432/projectgoat
# Railway/Render: Automatically set by platform
DATABASE_URL=sqlite:///projectgoat.db

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Production origin (your deployed frontend URL)
# Example: https://projectgoat.yourdomain.com
# PRODUCTION_ORIGIN=

# Additional custom origins (comma-separated)
# Example: https://app1.example.com,https://app2.example.com
# CUSTOM_ORIGINS=

# =============================================================================
# SECURITY
# =============================================================================
# Session secret key (generate a strong random string for production)
# Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_SECRET=development-secret-change-in-production

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# Local Laptop Deployment:
#   - No .env file needed! Defaults work out of the box
#   - Uses SQLite (no admin rights required)
#   - Accessible only from localhost
#
# Online Production Deployment (Railway/Render):
#   - Set ENVIRONMENT=production
#   - Set HOST=0.0.0.0
#   - DATABASE_URL automatically provided by platform
#   - Set PRODUCTION_ORIGIN to your deployed URL
#   - Generate secure SESSION_SECRET
#
# Testing PostgreSQL Locally:
#   1. Install PostgreSQL
#   2. Create database: createdb projectgoat
#   3. Set DATABASE_URL=postgresql://localhost/projectgoat
#   4. Run: python run.py
